var GPRS = GPRS ? GPRS : KISSY.namespace('GPRS', true);

KISSY.Config.debug = true; 

KISSY.config({
    packages: {
        "gprs":{
            base: "/asserts/js",
            charset: "utf-8"
        }
    }
});